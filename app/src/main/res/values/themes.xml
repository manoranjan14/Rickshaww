<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="CustomMenuTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textColorPrimary">#000000</item> <!-- Black text color -->
        <item name="android:itemTextAppearance">@style/CustomMenuTextStyle</item>
        <item name="android:popupBackground">#FFFFFF</item> <!-- White background for popup menu -->
    </style>
    <style name="CustomMenuTextStyle" parent="TextAppearance.AppCompat.Widget.ActionBar.Menu">
        <item name="android:textColor">#000000</item> <!-- Black color for menu text -->
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />
</resources>